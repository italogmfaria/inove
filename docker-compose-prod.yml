version: '3.8'

services:
  inove-frontend:
    build: .
    ports:
      - "80:80"
    environment:
      - NODE_ENV=production
      - API_URL=${API_URL:-http://backend:8080/api}
    depends_on:
      - inove-backend
    restart: unless-stopped